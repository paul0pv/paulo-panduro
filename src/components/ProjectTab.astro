---
import type { Project } from '../lib/data';

interface Props {
    project: Project;
}

const { project } = Astro.props;
const { id, title, description, tools, learning, link } = project;

---

<article class="flex flex-col items-start bg-lapislazuli-600 text-bw-200 p-6 rounded-md shadow-lg">
        <h3 class="text-xl font-bold mb-2">{title}</h3>
        <p class="text-bw-400">{description}</p>
        <!--div class="flex gap-2 justify-evenly mt-4">
            {tools.map((tool: string) => (
                <span class="bg-steelBlue-900 text-white px-2 py-1 rounded-2xl text-sm">{tool}</span>
            ))}
        </div-->
        <a href={link} target="_blank" class="text-blue-500 hover:underline mt-4 block">View Project</a>
    </article>